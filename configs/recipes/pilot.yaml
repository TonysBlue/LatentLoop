name: pilot
dataset: pilot
stages:
  - name: pretrain
    config: ../stages/pilot-pretrain.yaml
  - name: sft
    config: ../stages/pilot-sft.yaml
  - name: rl
    config: ../stages/pilot-rl.yaml
evaluation:
  validation_after_each_stage: true
  test_after_final_stage: true
