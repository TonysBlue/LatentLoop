name: pilot
dataset: pilot
initial_checkpoint: ~/latentloop-data/checkpoints/base/state-loop.pt
stages:
  - name: head
    config: ../stages/pilot-head.yaml
  - name: joint
    config: ../stages/pilot-joint.yaml
evaluation:
  validation_after_each_stage: true
  test_after_final_stage: true
