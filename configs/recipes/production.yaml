name: production
dataset: production
initial_checkpoint: ~/latentloop-data/checkpoints/base/state-loop.pt
stages:
  - name: head
    config: ../stages/production-head.yaml
  - name: joint
    config: ../stages/production-joint.yaml
evaluation:
  validation_after_each_stage: true
  test_after_final_stage: true
