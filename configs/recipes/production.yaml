name: production
dataset: production
stages:
  - name: pretrain
    config: ../stages/production-pretrain.yaml
  - name: sft
    config: ../stages/production-sft.yaml
  - name: rl
    config: ../stages/production-rl.yaml
evaluation:
  validation_after_each_stage: true
  test_after_final_stage: true
