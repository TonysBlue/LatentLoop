[project]
name = "latentloop-contracts"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = ["protobuf>=5,<8"]

[build-system]
requires = ["hatchling>=1.27"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/contracts"]
